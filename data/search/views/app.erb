<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Library Search</title>
    <link rel="stylesheet" href="./bootstrap.min.css"/>
    <script src="./bootstrap.min.js"></script>
  </head>
  <body class="bg-light">
    <div class="container my-3">
      <div class="py-3">
        <h1>Library Search</h1>
      </div>
      <div class="row">
        <div class="col mb-3">
          <div class="card card-body shadow d-inline-block">
            <form>
              <div class="form-group">
                <input name="title" type="text" placeholder="Title" value="<%= params['title'] %>" class="form-control"/>
              </div>

              <div class="form-group">
                <input name="author" type="text" placeholder="Author" value="<%= params['author'] %>" class="form-control"/>
              </div>

              <input type="submit" class="btn btn-primary btn-block">
            </form>
          </div>
        </div>
      </div>

      <div class="row">
        <% @results.each do |book| %>
          <div class="col-4 mb-3">
            <div class="card h-100 shadow">
              <img src="http://localhost/book-<%= rand(1..3) %>.jpg" style="height: 120px; width: 120px">
              <div class="card-body">
                <h6><%= book['title'] %></h6>
                <p class="small"><%= book['author'] %></p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </body>
</html>